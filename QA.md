## AI 연결 완료 버튼 수정 (2026-09-12)

- 상단 **완료**와 키보드 **완료**가 입력한 API 키·제공자·모델을 저장한 후 설정 화면을 닫습니다. 저장 오류는 화면을 유지한 채 표시하며, **취소**는 미저장 입력을 폐기합니다.
- 기존 키를 다시 입력하지 않아도 모델 설정을 바꿀 수 있습니다. 재열기 시 저장 상태를 표시하고 키 입력란은 비워 둡니다.
- Debug 시뮬레이터 빌드와 API 요청 형식 검사 31개 통과. 연결 UI 검사 6개 확인: 상단 완료 저장·재열기, 키보드 완료, 잘못된 입력의 오류 표시, 취소, 기존 키를 보존한 모델 변경, Gemini 선택 저장.
- UI 검사는 별도 테스트 앱·Keychain 서비스·UserDefaults에서 가짜 키로 수행했습니다. Xcode가 시뮬레이터용 키체인 권한을 구성하며 개인 인증서·실제 키·유료 API 요청은 사용하지 않습니다.

재실행:

```sh
python3 scripts/check_pdf_import.py --live-ui --only-testing CanvasLiveInkTests/AIConnectionVisualTests
```

## 프로젝트 및 여백 AI (2026-09-10)

- [x] Core 검증 30개: 기존18 + 프로젝트5 + AI 저장소7. 구버전 라이브러리 호환, 프로젝트 할당·필터·삭제시 노트 보존, 대화 저장·재로드, 이미지/영역/노트·프로젝트 분리, 손상 파일 보존, 대화 삭제 격리.
- [x] API 형식·응답·취소 검사 31개: OpenAI/Gemini 이미지·텍스트·지침·대화, 인증 헤더, 빈 답변·차단·길이 제한·HTTP 오류, 전송 전 취소. 실제 키·네트워크 요청 없이 검사.
- [x] iPad 시뮬레이터 Debug 빌드. PDF 경계의 필기 합성, 90도 회전 PDF 텍스트, 영역 밖 텍스트 제외, 캡처 크기 제한, 프로젝트 이동 후 전송 차단, 질문 저장 실패·재저장도 통과.
- [x] 기존 필기 UI 회귀 3개: 긴 PDF 위·중간·아래 필기, 페이지 잔상, 반투명 획 지우개.
- [x] 새 UI 검사 2개: 영역 선택·이동·확정, 필기 이미지 픽셀 미리보기, 원형 아이콘 열기·닫기, 다른 페이지에서 핀과 기존 필기 분리, 원래 페이지 왕복, 작성 중 질문 보존.
- [ ] 실제 OpenAI/Gemini API 키로 첫 질문·후속 질문, 키 교체·해제, 모델 변경, 한도 오류.
- [ ] 실제 iPad에서 Apple Pencil 입력과 AI 선택 모드 왕복, 회전·작은 창·키보드 표시.

프로젝트마다 학습 지침이 공유되며 대화에는 선택 영역과 그 대화 기록만 전송합니다. 다른 노트 전체/다른 대화는 자동 전송하지 않습니다. 이전 프로젝트 대화는 목록에서 읽기 전용입니다. 키는 기기 전용 Keychain에 저장합니다. API 사용료는 별도이며, 일반 ChatGPT/Gemini 웹 로그인 연결은 구현하지 않습니다.

영역 이미지는 선택 시점의 PDF·필기·텍스트·사진 합성입니다. 영역 내 PDF 텍스트를 함께 추출하며 손글씨는 이미지로 전달합니다. AI 핀·채팅은 PDF/PNG 내보내기에 포함하지 않습니다. 노트 전체 요약·별표 우선 인식·강의 녹음은 후순위로 남겨 두었습니다.

# 검증 기록

최종 확인: 2026-09-12

## 획 지우기 미리보기 (2026-09-10)

- 획 지우개 접촉 중에는 원본 PKDrawing을 유지. 닿은 획은 불투명도 35%, 지우개 경로는 흰색으로 화면에만 표시하며 뗄 때 전체 획 삭제를 한 번 확정.
- 접촉 취소·페이지 전환·편집 종료 시 미리보기를 폐기하고 원본 유지. 펜의 늦은 압력 데이터가 도착하면 이전 스냅샷으로 덮어쓰지 않도록 지우기를 취소.
- 픽셀 지우개·펜 입력은 기존 PencilKit 인식기로 처리. 긴 노트의 뷰포트·확대·스크롤 좌표와 페이지별 렌더링 뷰 교체 방식을 유지.
- 획의 렌더링된 알파로 충돌 판정. 픽셀 지우개로 생긴 투명한 영역, 올가미로 이동한 획, 70,000포인트 아래의 필기, 빠르게 가로지르는 지우개 경로 및 저장 검증 통과.
- iPad (A16), iOS 26.5 지우개 UI 검사 통과: 누른 상태에서 반투명 획·흰색 경로의 픽셀 색 확인, 원본 drawing 유지, 뗀 뒤 전체 삭제, 실행 취소·다시 실행, 취소 시 원본 복원, 두 손가락 확대 후 필기 보존. 세 손가락 넘김 인식기를 켠 상태에서 검사.
- 전체 UI 회귀 검사 **3개 통과 / 실패 0**: 지우개, 긴 PDF의 위·중간·아래 실제 터치 필기, 빨간 필기·빈 페이지·파란 필기의 반복 전환 픽셀 검사. 실제 앱 Debug 시뮬레이터 빌드 성공.
- 실제 Apple Pencil 입력과 큰 필기 문서에서의 성능은 실기기 확인이 필요함.

## 페이지 전환 잔상 수정 (2026-09-10)

- 페이지 ID가 바뀔 때만 새 PencilKit 렌더링 뷰를 설치하고 이전 뷰·delegate·도구 관찰자를 분리. 같은 페이지의 저장/UI 갱신에는 캔버스를 교체하지 않음.
- 이전 캔버스에서 뒤늦게 도착한 필기·스크롤 이벤트를 무시. 이전 필기가 새 페이지에 저장되지 않도록 차단.
- 현재 펜 설정은 새 캔버스에 복원. 페이지 전체의 이전 필기 화면을 포함하던 전환 페이드 제거.
- 긴 노트의 네이티브 스크롤·확대·좌표 변환·저장 형식은 유지.
- 통합 검사: 빈 페이지, 필기 있는 페이지, 같은 페이지 갱신, 이전 callback 지연 도착, 펜 설정 보존, 원래 페이지 왕복 보존 검증 통과.
- 시각적 회귀 검사 통과: 빨간 필기 → 빈 페이지 → 파란 필기 → 빈 페이지 → 원래 페이지를 2회 반복하며 새 입력 없이 화면 픽셀 검사.
- 기존 긴 노트 실제 터치 필기 회귀 검사도 재통과. iPad (A16), iOS 26.5: UI 테스트 2개 통과 / 실패 0. 실제 앱 Debug 시뮬레이터 빌드 성공.

## 이어 붙인 PDF의 필기 표시 수정 (2026-09-09)

- 문서 높이 전체를 차지하던 PencilKit 뷰와 외부 확대 스크롤뷰를 제거. PencilKit은 화면 크기로 유지하고 자체 contentSize·zoomScale·contentOffset으로 긴 문서를 처리함.
- PDF 배경·항목 선택·드래그 좌표는 PencilKit과 동일한 변환을 사용. 기존 저장된 필기 좌표나 파일 형식은 변경하지 않음.
- 저장 갱신 시 입력 인식기·팔레트·first responder를 반복 설정하지 않도록 처리.
- 75,520포인트 문서: 3개 확대 배율 × 위·중간·아래 위치에서 반복 UI 갱신 후 좌표·확대율·기존 drawing 데이터 유지 검증 통과.
- iPad (A16), iOS 26.5 XCUITest: 위·중간·아래, 각각 2회 실제 터치 획 입력. 터치 중 뷰포트 샘플, 펜을 뗀 뒤 기존 획 위치, 새 획 시작점, 획 수 검사 **1개 UI 테스트 통과 / 실패 0**.
- 이 자동 검증은 손가락 입력을 사용함. Apple Pencil 압력·기울기 및 실제 iPad에서의 실시간 표시 확인은 별도로 필요함.

```sh
python3 scripts/check_pdf_import.py --live-ui
```

## 이 환경에서 확인한 항목

- iPad (A16), iOS 26.5 시뮬레이터 별도 테스트 앱: 회전·서로 다른 크기의 PDF 준비, 두 가지 가져오기, 이어 붙인 배경 색상/순서, 경계를 가로지르는 필기 저장·복원, PDF·PNG 내보내기, 노트 복제 검증 통과. 연속 PDF 편집 화면 표시 확인.
- Swift Core 실행 검증: **18 / 18 통과**.
- 앱 Swift 소스 전체: `swiftc -frontend -parse` 문법 검사 통과.
- `project.pbxproj`, `Info.plist`, `PrivacyInfo.xcprivacy`: `plutil -lint` 통과.
- 앱 아이콘: 1024 × 1024 PNG, 알파 채널 없음, 시각적 확인 완료.
- Xcode 26.6, iOS Simulator SDK 26.5: `NoteMargin` 스킴 Debug 빌드 성공 (`CODE_SIGNING_ALLOWED=NO`, 2026-09-08).

Core 검증은 연속 PDF의 순서·서로 다른 높이·저장 복원, 페이지별 가져오기와 1페이지 처리, 이전 형식 호환성, 잘못된 PDF 크기 거부, 이름 변경 후 기존 노트·필기·PDF 보존, 이전·새 보관함 공존 시 덮어쓰기 방지, 빈 보관함, 메타데이터 저장·재읽기, 필기 바이트 교체·재읽기, 독립적인 복제본, 손상 파일 보존, 스키마 버전 거부, 파일 경로 검사, 폴더 삭제 후 노트 보존, 휴지통 필터·복원, 텍스트 검색·폴더 필터, 정렬, 잘못된 숫자의 저장 실패 시 이전 데이터 보존을 다룹니다.

**아래 체크하지 않은 동작 검증은 아직 수행하지 않았습니다.** Xcode 설치 후 앱 전체의 iOS SDK 타입 검사와 시뮬레이터용 Debug 빌드는 통과했으며, 새 연속 PDF 화면은 시뮬레이터에서 확인했으며, 제스처 입력·실기기 검증은 남아 있습니다. 리드미의 이미지는 구현을 참고해 만든 디자인 미리보기입니다.

PDF 통합 검증 재실행 (별도 테스트 앱 사용):

```sh
python3 scripts/check_pdf_import.py
```

## Xcode 설치 후 확인

- [x] Debug 시뮬레이터 앱 빌드 성공.
- [ ] Release 앱 빌드 및 경고 검토.
- [ ] iPad 시뮬레이터에서 빈 보관함 → 새 노트 생성 → 편집 화면 → 닫기.
- [ ] 노트 생성 시 설정 시트가 닫힌 다음 편집 화면이 표시됨.
- [ ] 펜·연필·형광펜·지우개·올가미·자·색상·굵기 변경.
- [ ] 획 여러 개 입력 → 실행 취소 → 다시 실행 → 페이지 전환 후 페이지별 기록 분리.
- [ ] 획 입력 직후 페이지 전환·앱 비활성화 → 다시 열어 필기 보존 확인.
- [ ] 확대·축소·용지 맞추기, 회전 및 작은 창 크기에서 좌표와 필기 일치.
- [ ] 손가락 필기 설정에 따른 한 손가락/두 손가락 화면 이동.
- [ ] PDF 세로·가로·90도 회전·비표준 크기 페이지의 배경 및 필기 정렬.
- [ ] 암호가 잠긴 PDF, 손상된 PDF를 가져올 때 오류 표시.
- [ ] 파일 앱의 PDF 공유/열기에서 새 노트로 가져오기.
- [ ] 텍스트와 사진 삽입, 크기 편집, 이동, 삭제 및 재실행 후 유지.
- [ ] 페이지 추가·복제·순서 변경·삭제, 마지막 한 페이지 삭제 금지.
- [ ] 노트·폴더 이름 변경, 이동, 즐겨찾기, 검색과 세 가지 정렬.
- [ ] 노트 복제 후 사본의 필기가 원본을 바꾸지 않음.
- [ ] 폴더 삭제 후 노트 유지, 휴지통 복원, 영구 삭제 확인창.
- [ ] PDF 내보내기 결과를 외부 PDF 뷰어에서 열어 모든 페이지·방향·필기·이미지 확인.
- [ ] 현재 페이지 PNG 공유, 파일 앱으로 저장.
- [ ] 저장 공간 부족 시 오류 표시 및 메모리 필기 유지, 여유 공간 확보 후 재저장.
- [ ] 손상된 `.drawing` 파일 또는 누락된 PDF 원본을 빈 페이지로 덮어쓰지 않음.
- [ ] 다크 모드, 큰 텍스트 크기, VoiceOver 레이블, 외장 키보드 단축키.

## PDF 가져오기 방식과 페이지 넘김

- [ ] 파일 앱에서 1페이지 PDF 가져오기 → 선택 없이 열림.
- [ ] 2페이지 이상 PDF → 두 가지 방식 표시; 취소 시 노트가 추가되지 않음.
- [ ] 다른 앱의 PDF 열기로도 같은 선택 화면 표시; 이미 열린 노트의 필기 보존.
- [ ] 이어 붙인 PDF에서 첫 페이지부터 마지막 페이지까지 스크롤, 페이지 경계를 가로지르는 필기.
- [ ] 페이지별 보기에서 세 손가락 왼쪽 → 다음, 오른쪽 → 이전; 첫·끝 페이지 경계.
- [ ] 세 손가락 넘김 시 필기 획이나 실행 취소가 발생하지 않음; 한 손가락 필기·두 손가락 확대 동작 유지.
- [ ] 안내가 자동으로 사라지고 하단 손 아이콘으로 다시 표시됨; 동작 줄이기 설정 존중.
- [ ] 여러 크기·회전 페이지를 이어 붙인 후 사진·텍스트 배치, 저장 후 재실행, PDF·PNG 공유.
- [ ] 수백 페이지 PDF의 스크롤·필기 응답성 및 메모리 측정.

## 실제 iPad와 Apple Pencil에서 확인

- [ ] Apple Pencil 필기 지연, 압력·기울기, 손바닥 접촉 시 의도하지 않은 선 발생 여부.
- [ ] PencilKit 도구 팔레트가 화면 크기·방향 변화와 시트 열기·닫기 후 정상 표시됨.
- [ ] 확대 상태에서도 필기와 용지/PDF의 좌표가 일치함.
- [ ] 긴 필기 세션 후 저장, 앱을 다시 시작한 뒤 데이터 보존.
- [ ] 수십 페이지 PDF와 큰 사진의 가져오기·이동·내보내기 시 메모리와 응답성 측정.

## 현재 범위와 제약

- 단일 기기 로컬 보관함이며 계정·동기화·협업 기능은 없음.
- 실행 취소·다시 실행은 현재 페이지의 필기 세션에 적용. 텍스트·사진·페이지 삭제는 별도 작업.
- 필기와 입력 텍스트·이미지의 레이어는 분리되며, 텍스트·이미지는 필기 아래에 배치됨.
- PDF 내보내기 시 필기 레이어는 래스터 이미지로 합성됨.
- 파일 I/O, PDF 가져오기·렌더링·내보내기가 주 스레드에서 수행되므로 큰 문서는 성능 측정 후 백그라운드 처리 개선이 필요할 수 있음.
- 앱 배포용 서명, TestFlight/App Store 제출, 서명된 설치 파일 생성은 미수행.


## 2026-09-15 — API / 개인용 ChatGPT 버전 분리

- `NoteMargin`: 기존 API 앱 ID와 화면 유지. Debug 시뮬레이터 빌드 통과.
- `NoteMarginPersonal`: `PERSONAL_CHATGPT` 조건으로 실제 ChatGPT WKWebView 화면 사용. Debug 및 Release 시뮬레이터 빌드 통과. 같은 iPad 시뮬레이터에 두 앱 설치 확인; 한국어 앱 이름도 별도로 확인.
- Core 33/33 통과: 기존 노트 호환성, 대화 URL 저장/복원, 인증 주소 거부와 쿼리 제거, 다른 프로젝트 지침 배제 포함.
- API wire 31/31 통과. 네트워크/API 과금 요청 없음.
- 개인용 native integration 통과: 실제 WKWebView에 로컬 HTML을 표시하고 SPA 경로 변경 → 여백 대화 URL 저장 확인, 개인용 `send`의 API 호출 차단, 테스트용 비지속 웹 저장소 확인. 실제 ChatGPT에 로그인한 검사는 아님.
- 개인용 XCUITest 복사 버튼/패널 토글 검사는 추가했으나 이 Mac에서 테스트 러너가 시작되지 않고 LLDB 버전 조회 오류로 대기하여 완료하지 못함. 통과로 계산하지 않음. 위 native integration은 테스트 러너 없이 앱을 직접 실행해 완료함.
- 기존 `Canvas` 필기/페이지 전환/지우개 파일은 수정하지 않음.
- 이 Mac의 Xcode 빌드 서비스가 `clang -v -E -dM`의 verbose stderr 파이프에서 멈춰, 검증 시에만 임시 `CC` 어댑터로 해당 탐색 명령의 내부 cc1 진단 출력을 생략함. 실제 버전·매크로와 앱 컴파일러는 유지. 저장소 프로젝트에는 어댑터 경로나 빌드 설정을 넣지 않음.
- 실제 계정 로그인, 로그인 유지 후 재실행, Google/Apple 계정 로그인, 실제 ChatGPT 입력칸의 이미지 붙여넣기·PNG 첨부·전송은 실기기 수동 확인 필요. 내장 브라우저 로그인 거부 시 Safari 대안은 별도 세션임.

```sh
swift run CoreChecks
python3 scripts/check_pdf_import.py --personal
python3 scripts/check_pdf_import.py --personal --live-ui
```

## 2026-09-15 — 개인용 웹 입력 자동화

- 개인용 여백 창의 ‘ChatGPT로 보내기’가 PNG 첨부 → 질문/프로젝트 지침/추출 텍스트 입력 → 표시된 이미지 미리보기·전송 가능 상태 확인 → 전송 버튼 한 번 클릭을 수행함. 비공개 HTTP API나 로그인 세션 토큰을 사용하지 않음.
- 실제 로그아웃 상태의 ChatGPT 모바일 웹에서 입력 폼, 파일 입력, 전송 버튼의 DOM 속성을 확인함. 로그인된 실제 계정의 파일 업로드/메시지 전송은 수행하지 않음.
- 실제 WKWebView + 로컬 HTML fixture 검사 10개 통과: 이미지와 따옴표/줄바꿈 질문 전달 및 1회 전송, 로그인 필요 시 중단, 기존 초안 보호, 첨부 미확인 시 중단, 업로드 진행 중 전송 금지, 업로드 오류 시 전송 금지, 기존 PDF 첨부 보호, 준비된 질문 재전송 방지, 처리 중 중단, 작업 시작 전 즉시 중단. 네트워크 호출/유료 요청 없음.
- 개인용 Release 시뮬레이터 빌드 통과. 앞 절의 Xcode 탐색 출력 어댑터를 검증 시에만 사용함.
- 웹 UI 변경에 영향을 받는 실험 기능이며 실제 로그인 계정에서 이미지 미리보기 구조·업로드 완료 표시·전송 여부의 수동 검증이 남아 있음. 결과가 불확실할 때 자동 재시도하지 않음.
